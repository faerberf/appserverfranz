"""Setup script for the appserver package."""
from setuptools import setup, find_packages

setup(
    name="appserver",
    version="0.1",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
